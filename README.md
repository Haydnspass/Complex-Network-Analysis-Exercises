# Complex-Network-Analysis-Exercises

## Setup environment
- requires conda

```bash
conda env create -f environment.yaml

# or update
conda env update -f environment.yaml
```
select the respective kernel in jupyter.